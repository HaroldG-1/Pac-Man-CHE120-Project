main is the "original" branch where none of our changes are made
push and pull onto the edge branch.
any changes made here will be merged with edge and then rolled back

git fetch
git pull
git checkout edge
